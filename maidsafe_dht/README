Directory structure 

src	  -	Maidsafe source files and build environment 
wrappers  -	CPP wrappers and cython file to generate interface modules and libraries 


Steps to install the dependecy and tokyocabinet sources to maidsafe:

1)	cd dependencies/tokyo_cabinet

2)	gunzip all the files and extract it.
	eg: gunzip *.gz 

3)	Configure compile and install all 3 libraries

	tar xvf bzip2-1.0.5
	cd bzip2-1.0.5
	make all
	make install
	tar xvf zlib-1.2.3

	cd ../zlib-1.2.3/
	configure
	make all
	make install

	cd ../tokyocabinet-1.4.30/
	configure
	make all
	make install

4)	cd ../../dependencies

5)	Extract the unix sources

6)	Run the script 'linux_build.sh' to install the respective dependencies on linux


Steps To Build files and generate maidsafe and interface modules on Linux machines:

1)	cd src/build/Linux

2)	Run the cmake with the script 'mkcodeblockslinux.sh'
	By default it will create the makefiles in Release mode. To build in  debug
	mode change "-DCMAKE_BUILD_TYPE=Debug"

3)	Now build using 'make' command

4)	cd ../../../wrappers

5)	Install the cython and python-dev packages inorder to generate the interface
 	modules in python.

	eg: apt-get install cython
	    apt-get install python-dev(Ubuntu systems)

6)	Run the the 'link_so_Release' script  
	./link_so_Release.sh
	This will generate a library libc_interface_dht.so, which links to maidsafe database.
	Also generates a python interface module(dht.so), which can subsequently be imported from Python (OSIS) code.
	

Limitations:
	
	Inorder to import the final python moudule(dht.so) in qshell, We have to build on the machines
	where python2.5 is installed

	However Once we build interface module on the above said machines, still we can import it on greater verisons of
	python installed
