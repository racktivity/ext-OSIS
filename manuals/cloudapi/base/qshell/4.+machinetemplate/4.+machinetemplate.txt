h2. {{PyMonkey Cloud API DOC for cloud object}} machinetemplate
{multi-excerpt:name=machinetemplate.list}
h3. {{machinetemplate.list()}}
List of all available machine templates

h4. {{Example}}
{code}
cloudApiConnection.machinetemplate.list()
{code}

h4. {{Return}}
tuple of machine template names

{multi-excerpt}

{multi-excerpt:name=machinetemplate.getDetails}
h3. {{machinetemplate.getDetails(machineTemplate)}}
Get the details of a machine template

h4. {{Parameters}}
* machineTemplate (string): Name of the template

h4. {{Example}}
{code}
cloudApiConnection.machinetemplate.getDetails(machineTemplate)
{code}

h4. {{Return}}
dict, \{name:string, description: string, Type: string, OS: string, capacityProperties: \{propertyName(string): value(string), ...\}, properties: \{propertyName(string): value(string), ...\}, Ports:\{portName(string): \{Type: string,capacityProperties: \{propertyName(string): value(string), ...\}\}, ...\}\}

{multi-excerpt}
