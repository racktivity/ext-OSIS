h2. {{Email Cloud API DOC for cloud object}} portal
{multi-excerpt:name=portal.getVersion}
h3. {{portal.getVersion()}}
This function is only to check the version of the CloudAPI

h4. {{Example}}
{noformat}
login = john
passwd = secret
category = portal
action = getVersion
space = John Doe
{noformat}

h4. {{Return}}
string, Version of the CloudAPI

{multi-excerpt}
