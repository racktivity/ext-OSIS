
cable.getConnectedDevices()  #returns array of 2 device objects being connected by cable
backplane.getConnectedCables()
backplane.getConnectedDevices()
backplane.getConnectedMachines()     #return all machines connected to backplanes

machine.getConnectedBackplanes()
machine.getHostDevice()
machine.getConnectedLans()
machine.getApplications()  #return array of all applications
machine.getIPaddresses()  #return array of all ipaddress  objects (sub obj from machine, @question can it be changed then?)
 
lan.getConnectedMachines()     #return all machines connected to lan
lan.getIpAddresses()  #return array of ip address strings

application.getMachine() #return machine object

h2. remarks

* use views where possible, if views don't exist create them
* always return the rootobjects unless if specified differently
